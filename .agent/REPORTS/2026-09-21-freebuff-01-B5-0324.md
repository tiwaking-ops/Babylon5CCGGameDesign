---
document:
  title: "B5-0324 report — cost-aware AI scoring (Finding 6)"
  status: "Report"
provenance:
  author_llm: {name: "Buffy", version: "deepseek-v4-flash"}
  assessor_llm: []
  last_modified_by_llm: {name: "Buffy", version: "deepseek-v4-flash"}
  created_date: "2026-09-21"
---

# B5-0324 — AI influence-cost scoring

Agent: `Buffy (deepseek-v4-flash)`. Toolchain: `javac 1.8.0_292`. Date: 2026-09-21.

## Deliverables (files in claim scope)

- **`ai/AIPlayer.java`** — MEDIUM and HARD now subtract card costs through
  the B5-0323 plumbing:
  - `RECRUIT_CHARACTER`: MEDIUM `max(0, 5 − recruitCost(p, ch))` (floored;
    the offer gate already guarantees affordability); HARD raw subtraction.
  - `PLAY_CARD`: positional base (agenda 8/9, location 6/7, group 4/5,
    enhancement 4, event 3) minus `card.getCost()` — MEDIUM floored, HARD raw.
  - `PROMOTE_CHARACTER`: already cost-aware since B5-0321 (raw cost minus).
  - Untouched by design (B5-0315 §2.4): `BUILD_INFLUENCE` (fixed 3 influence,
    not a card cost) and conflicts (no card cost).
  - Zero-cost invariance: with the current all-zero data, every score is
    exactly its pre-B5-0324 value, so AI behavior today is unchanged.
- **`engine/HeadlessConformanceTest.java`** — new AIS section, 7 checks driven
  through the **public** `chooseAction` API (deterministic for MEDIUM/HARD —
  no private-method reflection): zero-cost ordering preserved, a cost raise
  flips the choice, MEDIUM floors at 0 (PASS wins when everything floors),
  HARD subtracts, and recruit preference flips when costs flip. Suite: 74 →
  **81 checks, all PASS**.

## Verification

- Conformance suite **81/81 PASS**, exit 0.
- `RUN_TESTS=1 sh compile.sh` green end-to-end (build + suite + smoke, exit 0).
- Java 6 construct grep on both touched files: clean.

## Notes

- The scoring is live but dormant until a data-backfill task assigns cost
  values; the suite's cost-flip checks prove the mechanism so the backfill
  needs no further code work.
- EASY remains random by design (the difficulty contract).

## Scope discipline

Only the two claimed files touched; no engine/model changes. No commit made
(not requested).
