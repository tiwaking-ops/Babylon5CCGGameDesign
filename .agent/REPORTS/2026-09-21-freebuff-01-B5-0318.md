---
document:
  title: "B5-0318 report — de_agenda_seizing_advantage set PREMIERE → DELUXE (single-field data fix)"
  status: "Report"
provenance:
  author_llm: {name: "Buffy", version: "deepseek-v4-flash"}
  assessor_llm: []
  last_modified_by_llm: {name: "Buffy", version: "deepseek-v4-flash"}
  created_date: "2026-09-21"
---

# B5-0318 — single-field data fix: de_agenda_seizing_advantage

Agent: `Buffy (deepseek-v4-flash)`. Toolchain: `javac 1.8.0_292`. Date: 2026-09-21.

## Change

`b5ccg/resources/cards/deluxe.json`, record `de_agenda_seizing_advantage`
(line 122): `"set":"PREMIERE"` → `"set":"DELUXE"`. One field, one record. The
anchor string `"set":"PREMIERE","imageKey":"seizing_advantage"` was unique —
deluxe.json contained exactly one PREMIERE set-value — so no other record could
be touched. The record is otherwise byte-identical; total dataset remains 829.

## Why (provenance of the defect)

- B5-0311 addendum (freebuff-01): the `de_` id prefix, deluxe.json placement,
  and the card's own text ("Deluxe major change: now explicitly labelled Major
  Agenda") all say Deluxe; the set field said Premiere.
- `DeckLoader.loadFromResource` reads the set field literally (absent →
  PREMIERE default), so the card loaded as PREMIERE from deluxe.json.
- B5-0320 recorded the residual as "382 DELUXE + 64 PREMIERE-set; expected
  post-fix split 383/63" — this task delivers exactly that split.

## Verification

Data (grep):
- `"set":"PREMIERE"` occurrences in deluxe.json: 1 → **0**
- `"set":"DELUXE"` occurrences: 382 → **383**
- `"id":"` records across both card files: **829** (unchanged)

Live loader (git-ignored scratch runner `out/scratch5/Verify0318.java`, deleted
after verification — B5-0204/B5-0312 precedent):
- `loadBothSets()` = 446 cards = 383 DELUXE + 63 PREMIERE + 0 other
- exactly **one** "Seizing Advantage" in the pool — the deluxe copy; the
  premiere twin is title-deduped by `loadBothSets`' B5-0320 dedup. That dedup
  keys on TITLE, not set, so this fix changes the surviving copy's set identity,
  not pool membership.
- pool copy `getCardSet()` = DELUXE
- deluxe.json records with non-DELUXE set: **0** (of 383)

Gates:
- `RUN_TESTS=1 sh compile.sh` → exit 0 (build + B5-0308 conformance suite +
  B5-0201 smoke, all green)
- Java 6 construct grep on scope: 0 matches (data file; the rule is src-only —
  recorded as n/a)

## Scope discipline

Only `b5ccg/resources/cards/deluxe.json` (one field) touched. No `src/`
changes. Scratch verifier compiled and run only from git-ignored `out/` and
deleted afterward. No commit made (not requested).

## Coordination notes

- B5-0317 was OPEN in the ledger but held a live claim file (agent `Inkling`);
  per the one-writer rule it was left untouched. B5-0318 is fully independent
  (different file, different record).
- With this fix, every record in deluxe.json now carries set=DELUXE.
