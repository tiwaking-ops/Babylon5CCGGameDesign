---
document:
  title: "B5-0101 report — model/ Java 6 conversion"
  status: "Report"
provenance:
  author_llm: {name: "Solar Pro4", version: "solar-pro4:free"}
  assessor_llm: []
  last_modified_by_llm: {name: "Solar Pro4", version: "solar-pro4:free"}
  created_date: "2026-09-21"
  last_modified_date: "2026-09-21"
---

# B5-0101 report — model/ Java 6 conversion

**Date:** 2026-09-21  
**Agent:** Solar Pro4 (solar-pro4:free)  
**Task:** B5-0101 — Convert `model/` to Java 6

## Toolchain

- `javac -version`: `1.8.0_292`
- Build gate: `javac -source 6 -target 6`, stdlib only

## Files touched (7 of 23)

### Player.java
- 7× `new ArrayList<>()` → `new ArrayList<X>()` (diamonds)
- `innerCircle`, `supportingRole`, `fleets`, `locations`, `groups`, `enhancements`, `hand`

### Deck.java
- 3× `new ArrayList<>()` / `new LinkedList<>()` → explicit type args
- `drawPile`, `discardPile`, local `hand` in `draw(int)`, local `result` in `peekTop(int)`

### GameState.java
- 2× `new ArrayList<>()` → `new ArrayList<String>()` / `new ArrayList<Player>()`
- Replaced `players.stream().filter(Player::isHuman).findFirst().orElse(...)` with a for-loop in `getHumanPlayer()`

### Conflict.java
- Replaced `computeIfAbsent`/`putIfAbsent`/`getOrDefault` (all Java 8+) with manual `containsKey`/`put` checks
- `new LinkedHashMap<>()` → `new LinkedHashMap<Player, List<Card>>()`
- `new ArrayList<>()` in constructor → `new ArrayList<Card>()`
- Replaced `committed.getOrDefault(p, Collections.emptyList())` in `playerTotal` with `containsKey` guard

### AgendaCard.java
- Replaced `switch` on `String` (Java 7+) with chained `if ("KEY".equals(winConditionKey))` blocks
- Replaced `stream().mapToInt(FleetCard::getMilitary).sum()` with manual for-loop accumulation
- Replaced `stream().filter(p -> p != owner).allMatch(...)` with manual for-loop + early-exit flag

### CardEffect.java
- Removed `@FunctionalInterface` annotation (Java 8)

## Files NOT touched (no Java 6 violations)

`AftermathCard`, `Card`, `CharacterCard`, `ConflictCard`, `EventCard`, `EnhancementCard`,
`FleetCard`, `GameAction`, `GroupCard`, `LocationCard`, all 8 enum files — clean.

## Verification

- `javac -source 6 -target 6 -d out_model $(find ./src/b5ccg/model -name "*.java")`: **exit 0**, 25 `.class` files produced, only the expected bootstrap classpath warning.
- `grep -nE '->|::|stream\(|computeIfAbsent|putIfAbsent|getOrDefault|@FunctionalInterface|\\bnew\\b.*<>' model/*.java`: **empty** across all 23 files.

## Result

**DONE.** B5-0101 closed. All model/ files Java 6-clean.

## Claim

Deleted on completion.
